using UnityEngine;

public class CellTyping : MonoBehaviour
{
    /*
    private GridBuilder grid;
    private SelectionMovement selection;

    private bool isTyping = false;
    private string typingBuffer = "";
    //private CellData activeCell => grid.cell_datas[col, row];
    
    void Start()
    {
        grid = FindAnyObjectByType<GridBuilder>();
    }

    void Update()
    {
        
    }

    void BeginTyping(string firstCharacter)
    {
        isTyping = true;
        typingBuffer = firstCharacter;
        //activeCell.update_text(typingBuffer);
    }

    void CommitTyping()
    {
        isTyping = false;
        //activeCell.update_text(typingBuffer);
    }
    */
}
